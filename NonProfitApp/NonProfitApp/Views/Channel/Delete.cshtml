@model Channel
<div class="card" style="width: 18rem;">
    <div class="card-body">
        <h5 class="card-title text-danger">Are you sure you want to delete this channel?</h5>
        <p class="card-text">@Model.ChannelType</p>
    </div>
    <div class="card-body">
        <a asp-action="Index" asp-controller="Channel" class="btn btn-outline-primary">No</a>
        <form asp-action="Delete" method="post">
            <input asp-for="ChannelId" type="hidden" />
            <input class="btn btn-outline-danger" type="submit" value="Yes" />
        </form>
    </div>
</div>
