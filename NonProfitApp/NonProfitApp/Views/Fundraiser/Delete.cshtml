@model Fundraiser
<div class="card" style="width: 18rem;">
    <div class="card-body">
        <h5 class="card-title text-danger">Are you sure you want to delete this channel?</h5>
        <p class="card-text">@Model.FundraiserName</p>
    </div>
    <div class="card-body">
        <a asp-action="Index" asp-controller="Fundraiser" class="btn btn-outline-primary">No</a>
        <form asp-action="Delete" method="post">
            <input asp-for="FundraiserId" type="hidden" />
            <input class="btn btn-outline-danger" type="submit" value="Yes" />
        </form>
    </div>
</div>
