@model Donation

<div class="card" style="width: 18rem;">
    <div class="card-body">
        <h5 class="card-title text-danger">Are you sure you want to delete this donation?</h5>
        <p class="card-text">@Model.DonationDate</p>
        <p class="card-text">@Model.DonationAmount</p>
    </div>
    <div class="card-body">
        <a asp-action="Index" asp-controller="Donation" class="btn btn-outline-primary">No</a>
        <form asp-action="Delete" method="post">
            <input asp-for="DonationId" type="hidden" />
            <input class="btn btn-outline-danger" type="submit" value="Yes" />
        </form>
    </div>
</div>
